# LLM Providers
openai>=1.30
anthropic>=0.25
boto3>=1.34                  # Bedrock + S3 + Secrets Manager

# API Framework
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Pydantic — API layer ONLY (not in agent/tool layer)
pydantic>=2.7
pydantic-settings>=2.3

# Storage
redis>=5.0
psycopg2-binary>=2.9
sqlalchemy>=2.0

# Vector Store
qdrant-client>=1.9          # Unified client for local + cloud + self-hosted
pgvector>=0.2                # Prod

# Web Tool
httpx>=0.27
tavily-python>=0.3

# Embeddings
tiktoken>=0.7
sentence-transformers>=3.0

# Observability
structlog>=24.0
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp>=1.25
prometheus-client>=0.20

# Infrastructure as Code
# (Terraform itself is not a Python lib — installed separately)

# UI
streamlit>=1.35

# Retry + Resilience
tenacity>=8.3

# Config + Env
python-dotenv>=1.0
pyyaml>=6.0

# Prompt Templating
jinja2>=3.1

# Dev + Testing
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
bandit>=1.7
httpx                        # Also used for FastAPI test client